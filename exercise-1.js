import React from "react";
import ReactDOM from "react-dom";

//render only takes 1 HTML element
ReactDOM.render(
  <div>
    <h1>Hello World! This is my first react project.</h1>
    <p>I think React is pretty fun to learn.</p>
  </div>,
  document.getElementById("root")
);
